# Fix cycle 2 — from a live review of the deployed page (12:06) cross-checked against raw JPS/MET

Verified: the numbers are real (Sg. Tua 103.21 m matches JPS exactly). These are presentation bugs. Smallest diff each, commit each with `devin:` prefix.

1. **ENGLISH FIRST, BM SECOND — everywhere.** `web/index.html`: `lang="en"`, all labels/headings/buttons in English, with the BM version as the secondary line (smaller, muted). API `explanation`: `en` first, then `bm`. Pitch: English narration by default, BM toggle. The app name stays "Banjir".
2. **Status label bug.** Station list shows `Sg. Tua di Emp. Batu — WARNING` while level 103.21 is between alert 103.10 and warning 104.10 → must show **ALERT**. Find where the web/API maps status and make it use `jps.py`'s computed `status` field verbatim (NORMAL / ALERT / WARNING / DANGER / OFFLINE / UNKNOWN). Colour: green / amber / orange / red / grey. Check with `curl "$U/api/status?place=Gombak"` and the page.
3. **MET warnings must be scoped.** Today's 3 warnings are marine "Strong Winds and Rough Seas" for Sarawak / South China Sea — showing them under Gombak with no location reads as fabricated. Rule: show only warnings whose parsed `areas` (from `met.py`) mention the resolved state (or its districts / "Semenanjung" / "Peninsular"); otherwise render: "No MET warnings for WP Kuala Lumpur / Selangor right now. 3 active nationally (marine, Sarawak waters) — view all". Always print the warning's `areas` text under its title.
4. **"Next 24h" is empty on the page** although `met_forecast` is in the JSON. Render today's + tomorrow's `morning/afternoon/night` forecast in English (use the BM→EN map in `met.py`) with location name + "Source: MET Malaysia via data.gov.my".
5. **Relief centres wording.** When none in the resolved state: "No open relief centres in {state} right now. {n} open nationally ({states}, {disaster types}). Source: JKM InfoBencana" — using the live `pps.py` output.
6. **Atlantis still returns HTTP 500 on the deployed URL.** Reproduce with `curl -s -o /dev/null -w "%{http_code}" "$U/api/status?place=Atlantis"` and fix until 200 + `place_not_found: true` + 8 district suggestions. Add a test in `samples/03_unknown_place.txt` flow.
7. **Deploy loop is yours.** The Vercel CLI is logged in (`vercel whoami` → kroevasuperadmin). Run `vercel deploy --yes --scope kroevasuperadmins-projects`; if the build errors, read `vercel inspect --logs <url>` and fix until **Ready**. Then run the 3 curls (Gombak, Shah Alam, Atlantis) against the new URL and paste outputs. Do NOT run `--prod` — preview only; the human promotes. Write the final preview URL into `README.md` ("Live demo") and `DEMO.md`.
8. `git push origin master` when done. Print: preview URL, 3 curl outputs, commit hashes.
